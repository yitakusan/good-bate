---
description: 重要协作约束：上下文压缩、先测后改、测试数据只影子库、实库库存先验后改、路径变更须同步
alwaysApply: true
---

# 重要约束

## 上下文压缩

- 当本会话上下文用量接近或达到约 **80%** 时，主动提醒用户压缩/开启新会话，不要等用户发现再提。
- 提醒时用一句话说明：建议压缩对话或新开聊天，以免后续指令被截断。

## 新功能：先测试，确认后再改

- 添加或大改功能时：**先完成可验证的测试**（接口/页面冒烟、关键路径），把结果交给用户验收。
- **在用户明确说「验收通过 / 确认 / 可以改」之前**，不要把改动当成最终交付，也不要继续扩大范围改生产相关行为。
- 若可先在影子库、临时分支或只读验证上测，优先这样做；用户确认后再落最终实现或合并。
- 小修 typo、明显 bug fix、用户已明确要求「直接改」的任务不受此条限制。

## 测试数据：只在影子库操作

- 测试数据的创建、更改、删除**只允许**在影子库：`STOCKGOOD_DB_MODE=shadow` → `backend/data/stockgood.shadow.sqlite`。
- **禁止**在生产库（`backend/data/stockgood.sqlite` / `STOCKGOOD_DB_MODE=production`）写入、修改或删除测试订单、测试申请、测试货品或其它测试库存数据。
- 测试冒烟（建单、确认申请、改数量/运费/条码、进库出库等）**只在影子库**进行。
- 发现生产库已有测试脏数据时，应清理并提醒改用影子库。

## 实际库存变动：先影子库验收，再改实库

仅当操作会改变**实际库存**时，按下列顺序：

1. **优先在影子库完整测一遍**（同等创建 / 更改 / 删除路径与库存结果）。
2. **交给用户验收**；未获「验收通过 / 确认 / 可以改实库」前，禁止对生产库做对等库存写操作。
3. **用户确认后**，再在生产库执行实际库存更改。

生产库排查默认只读；任何写操作须用户明确授权，且不得留下测试数据。

## 路径更改：全量检测并同步关联引用

涉及文件/目录**重命名、移动、删除**或改动 import/路由/静态资源/配置中的路径时：

1. **先全库检索旧路径**（代码 import、路由、脚本、配置、文档、启动 bat、环境变量、相对路径等）。
2. **在所有关联文件中同步更新**到新路径，避免残留引用导致运行/构建 error。
3. **改完后再检一遍**：确认无旧路径残留；必要时空跑导入/启动或相关命令验证。
4. 若某引用无法同步（外部依赖、用户本地路径等），明确列出并告知用户。
