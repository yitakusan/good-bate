---
description: 重要协作约束：上下文压缩提醒、新功能先测后改、测试库存只改影子库
alwaysApply: true
---

# 重要约束

## 上下文压缩

- 当本会话上下文用量接近或达到约 **80%** 时，主动提醒用户压缩/开启新会话，不要等用户发现再提。
- 提醒时用一句话说明：建议压缩对话或新开聊天，以免后续指令被截断。

## 新功能：先测试，确认后再改

- 添加或大改功能时：**先完成可验证的测试**（接口/页面冒烟、关键路径），把结果交给用户验收。
- **在用户明确说「验收通过 / 确认 / 可以改」之前**，不要把改动当成最终交付，也不要继续扩大范围改生产相关行为。
- 若可先在影子库、临时分支或只读验证上测，优先这样做；用户确认后再落最终实现或合并。
- 小修 typo、明显 bug fix、用户已明确要求「直接改」的任务不受此条限制。

## 测试库存：只改影子库

- **禁止**在生产库（`backend/data/stockgood.sqlite` / `STOCKGOOD_DB_MODE=production`）写入测试订单、测试申请、测试货品或其它测试库存数据。
- 测试库存相关操作（建单、确认申请、改数量/运费/条码、进库出库冒烟等）**只允许**在影子库进行：`STOCKGOOD_DB_MODE=shadow` → `backend/data/stockgood.shadow.sqlite`。
- 若需在生产库排查，默认只读；任何写操作须用户明确授权，且不得留下测试数据。
- 发现生产库已有测试脏数据时，应清理并提醒改用影子库。
